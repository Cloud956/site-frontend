import React from "react";

interface Props {
  name: string;
}
const TransformationPanel = ({ name }: Props) => {
  return (
    <div>
      TransformationPanel
      
    </div>
  );
};

export default TransformationPanel;
